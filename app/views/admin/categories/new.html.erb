<h1>New Category</h1>

<%= form_for [:admin, @admin_category] do |f| %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', admin_categories_path %>
